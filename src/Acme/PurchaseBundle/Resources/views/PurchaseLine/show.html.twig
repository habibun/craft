{% extends '::base.html.twig' %}

{% block title %}PurchaseLine{% endblock %}

{% block body -%}
    <div class="row">
        <div class="col-lg-12">
            <section class="panel">
                <header class="panel-heading">
                    <strong>Transaction Number # {{ entity.id|capitalize }}</strong>
                <span class="tools pull-right">
                    <a class="icon-chevron-down" href="javascript:;"></a>
                    <a class="icon-remove" href="javascript:;"></a>
                </span>
                </header>
                <table class="table table-hover table-striped">
                    <tbody>
                    <tr>
                        <th>Transaction No</th>
                        <td>{{ entity.id }}</td>
                    </tr>
                    <tr>
                        <th>Product Name</th>
                        <td>{{ entity.product.name }}</td>
                    </tr>
                    <tr>
                        <th>Quantity</th>
                        <td>{{ entity.quantity }}</td>
                    </tr>
                    <tr>
                        <th>Price</th>
                        <td>{{ entity.price }}</td>
                    </tr>
                    <tr>
                        <th>Purchase Id</th>
                        <td>{{ entity.purchase.id }}</td>
                    </tr>
                    <tr>
                        <th>Purchase Date</th>
                        <td>{{ entity.purchase.purchaseDate|date('Y-m-d') }}</td>
                    </tr>
                    <tr>
                        <th>Supplier Name</th>
                        <td>{{ entity.purchase.supplier.name }}</td>
                    </tr>
                    <tr>
                        <th>Created At</th>
                        <td>{{ entity.purchase.createdAt|date('Y-m-d H:i:s') }}</td>
                    </tr>
                    <tr>
                        <th>Created By</th>
                        <td>{{ entity.purchase.createdBy }}</td>
                    </tr>
                    <tr>
                        <th>Updated At</th>
                        <td>{{ entity.purchase.updatedAt|date('Y-m-d H:i:s') }}</td>
                    </tr>
                    <tr>
                        <th>Updated By</th>
                        <td>{{ entity.purchase.updatedBy }}</td>
                    </tr>
                    <tr>
                        <th>Finalize Date</th>
                        <td>{{ entity.purchase.finalizeDate|date('Y-m-d H:i:s') }}</td>
                    </tr>
                    <tr>
                        <th>Finalize By</th>
                        <td>{{ entity.purchase.finalizedBy }}</td>
                    </tr>
                    </tbody>
                </table>
            </section>
        </div>
    </div>
{% endblock %}
