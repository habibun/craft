{% extends '::base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body -%}
    <div class="row">
        <div class="col-lg-12">
            <!--breadcrumbs start -->
            <ul class="breadcrumb">
                <li><i class="icon-home"></i> Dashboard</li>
            </ul>
            <!--breadcrumbs end -->
        </div>
    </div>
    {#{{ render(controller('AcmeDashBundle:Last:index', { 'max': 5 })) }}#}
    {#{% include 'AcmeDashBundle:Search:searchForm.html.twig' %}#}
    {{ render(controller('AcmeWidgetBundle:StateOverview:stateOverview')) }}
    {{ render(controller('AcmeWidgetBundle:WorkProgress:workProgress')) }}
    {{ render(controller('AcmeWidgetBundle:TopSearch:topSearchList')) }}

{% endblock %}
