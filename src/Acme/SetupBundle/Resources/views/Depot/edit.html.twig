{% extends '::base.html.twig' %}

{% block title %}Depot{% endblock %}

{% block body -%}
    <div class="row">
        <div class="col-lg-12">
            <!--breadcrumbs start -->
            <ul class="breadcrumb">
                <span class="text-info">You are here <span class="divider">»</span></span>
                <li><a href="{{ path('dashboard') }}"><i class="icon-home"></i>Dashboard</a></li>
                <li class="active"><a href="{{ path('depot') }}">Depot</a></li>
                <li class="active">Edit A Depot</li>
            </ul>
            <!--breadcrumbs end -->
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <section class="panel">
                <header class="panel-heading">
                    <strong>Depot edit</strong>
                <span class="tools pull-right">
                    <a class="icon-chevron-down" href="javascript:;"></a>
                    <a class="icon-remove" href="javascript:;"></a>
                </span>
                </header>
                <div class="panel-body">
                    {{ form_start(edit_form) }}
                    {{ form_row(edit_form.name) }}
                    <hr>
            <span class="pull-right">
                <a href="{{ path('depot') }}" class="btn btn-primary">Back</a>
            </span>
                    {{ form_widget(edit_form.submit) }}
                    {{ form_end(edit_form) }}
                </div>
            </section>
        </div>
    </div>
{% endblock %}
